route:
  group_by: ['alertname']
  receiver: 'panic_duty_webhook'
  group_wait: 5s
  group_interval: 10s
  repeat_interval: 1m

receivers:
  - name: 'panic_duty_webhook'
    webhook_configs:
      - url: 'http://panic_duty:8080/webhook'
        send_resolved: true
